<h1>Home</h1>
<% @notes.each do |note| %>
	<div id=<%= "#{note.id}" %>>
		<p><i><%= note.created_at %></i> <%= " - #{note.autor}" if note.autor %></p>
		<h3><%= note.title %></h3>
		<p><%= note.body %></p>
	</div>
<% end %>